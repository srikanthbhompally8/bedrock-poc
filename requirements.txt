# Core AWS SDK — provides the bedrock-runtime client used by bedrock_poc/client.py.
boto3>=1.34
# Web UI. Only needed to run app.py; the CLI works with boto3 alone.
streamlit>=1.32
# Loads variables from .env into the environment for cli.py / app.py.
python-dotenv>=1.0
# Structured output validation for resume parsing.
pydantic>=2.0
# PDF parsing for resume extraction.
pypdf>=4.0
# Database support — PostgreSQL adapter for persistent storage.
psycopg2-binary>=2.9
# SQL toolkit and ORM for database operations.
sqlalchemy>=2.0
# Database migration tool for schema management.
alembic>=1.12
